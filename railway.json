{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "/app/.venv/bin/uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
  }
}
